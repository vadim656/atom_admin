'use strict';

/**
 * tarif-elektroenergii service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tarif-elektroenergii.tarif-elektroenergii');
